---
# yaml-language-server: $schema=https://schema.blue-build.org/recipe-v1.json
# image will be published to ghcr.io/<user>/<name>
name: kyber
# description will be included in the image's metadata
description: >-
  KYBER OS — a dedicated gaming console built from a 3D-printed enclosure and a
  custom image derived from Bazzite. This is the base image: Bazzite plus the
  KYBER signing setup and file overlay, with no KYBER-specific software yet.

# the base image to build on top of (FROM) and the version tag to use
# ghcr.io/ublue-os/bazzite already ships Steam, Proton, gamescope and GPU drivers
base-image: ghcr.io/ublue-os/bazzite
image-version: stable # Bazzite's recommended channel; `testing` and `unstable` also exist

# module configuration, executed in order
# you can include multiple instances of the same module
modules:
  - type: files
    files:
      - source: system
        destination: / # copies files/system/* (* means everything inside it) into your image's root folder /

  - type: signing # this sets up the proper policy & signing files for signed images to work fully
